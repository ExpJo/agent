# Ranked Ideas

No ideas ranked yet. Run the pipeline:

```bash
npm run pipeline:framework
npm run pipeline:collect
npm run pipeline:rank
```

Or all at once:

```bash
npm run pipeline:all
```

Then open the UI: `npm run ui`
